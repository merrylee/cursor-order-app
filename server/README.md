# COZY server

커피 주문 앱 백엔드. Express + JavaScript.

## 실행

Node.js는 `D:\Documents\nodejs\` 를 사용한다.

```bash
cd server
npm install
npm run dev
```

서버는 http://localhost:3000 에서 열린다. 동작 확인: http://localhost:3000/api/health
