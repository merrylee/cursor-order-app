# 커피 주문 앱

## 1. 프로젝트 개요

### 1.1 프로젝트명
커피 주문 앱

### 1.2 프로젝트 목적
사용자가 커피 메뉴를 주문하고, 관리자가 주문을 관리할 수 있는 간단한 풀스택 웹 앱

### 1.3 개발 범위
- 주문하기 화면(메뉴 선택 및 장바구니 기능)
- 관리자 화면(재고 관리 및 주문 상태 관리)
- 데이터를 생성/조회/수정/삭제할 수 있는 기능

## 2. 기술 스택
- 프런트엔드: HTML, CSS, 리액트, 자바스크립트
- 백엔드: Node.js Express
- 데이터베이스: PostgreSQL

## 3. 기본 사항
- 프런트엔드와 백엔드를 따로 개발
- 기본적인 웹 기술만 사용
- 학습 목적이므로 사용자 인증이나 결제 기능은 제외
- 메뉴는 커피 메뉴만 있음

## 4. 프런트엔드 UI — 주문하기 화면

### 4.1 화면 목적
사용자가 커피 메뉴를 보고, 옵션을 선택한 뒤 장바구니에 담고, 주문을 제출하는 화면이다. 앱의 기본 진입 화면이며, 헤더에서 관리자 화면으로 이동할 수 있다.

### 4.2 레이아웃
화면은 위에서 아래로 다음 세 영역으로 구성한다.

1. **헤더**: 앱 이름과 화면 전환 내비게이션
2. **메뉴 목록**: 커피 메뉴 카드 그리드
3. **장바구니**: 담은 상품 목록, 총 금액, 주문 버튼

헤더는 화면 상단에 고정하고, 장바구니는 화면 하단에 항상 보이도록 한다. 메뉴 목록만 세로로 스크롤한다.

### 4.3 헤더
- 왼쪽: 앱 이름 `COZY`
- 오른쪽: `주문하기`, `관리자` 두 개의 내비게이션
- 현재 화면이 주문하기일 때 `주문하기`를 활성 상태로 표시한다 (테두리 등)
- `관리자`를 누르면 관리자 화면으로 이동한다
- 앱 이름과 `주문하기`를 눌러도 주문하기 화면에 머문다

### 4.4 메뉴 카드
메뉴는 가로로 나열된 카드로 보여 준다. 화면이 좁으면 다음 줄로 넘어간다.

각 카드는 다음을 포함한다.

| 요소 | 설명 |
| --- | --- |
| 이미지 | 메뉴 이미지. 없으면 플레이스홀더를 보여 준다 |
| 메뉴명 | 예: 아메리카노(ICE), 아메리카노(HOT), 카페라떼 |
| 기본 가격 | 원 단위, 천 단위 구분 (예: 4,000원) |
| 설명 | 짧은 한 줄 설명 |
| 옵션 | 체크박스로 선택. 옵션명과 추가 금액을 함께 표시 (예: 샷 추가 (+500원), 시럽 추가 (+0원)) |
| 담기 버튼 | 선택한 구성으로 장바구니에 추가 |

- 옵션은 메뉴마다 다를 수 있다. 옵션이 없는 메뉴는 체크박스를 보여 주지 않는다.
- 옵션은 여러 개를 동시에 선택할 수 있다.
- 담기 시점의 가격은 `기본 가격 + 선택한 옵션 추가 금액의 합`이다.
- 담기 후에는 해당 카드의 옵션 선택을 초기화한다.

### 4.5 장바구니
장바구니 영역 왼쪽 위에 `장바구니` 제목을 둔다.

#### 담은 상품 한 줄
- 표시 형식: `{메뉴명} ({선택한 옵션명, 쉼표로 구분}) X {수량}` 과 줄 금액
- 옵션이 없으면 괄호를 생략한다. 예: `아메리카노(HOT) X 2` / `8,000원`
- 옵션이 있으면 예: `아메리카노(ICE) (샷 추가) X 1` / `4,500원`
- 줄 금액은 `(기본 가격 + 해당 줄의 옵션 추가 금액) × 수량`이다.

#### 같은 구성의 처리
- 메뉴와 선택한 옵션 조합이 같으면 새 줄을 만들지 않고 수량을 1 늘린다.
- 옵션 조합이 다르면 별도 줄로 추가한다.

#### 총 금액과 주문
- 오른쪽: `총 금액 {합계}원` 과 `주문하기` 버튼
- 총 금액은 모든 줄 금액의 합이다.
- 장바구니가 비어 있으면 상품 목록 대신 안내 문구를 보여 주고, `주문하기` 버튼은 비활성화한다.
- `주문하기`를 누르면 현재 장바구니 내용으로 주문을 생성한다. 성공하면 장바구니를 비우고 완료 안내를 보여 준다. 실패하면 오류 안내를 보여 주고 장바구니는 유지한다.
- 결제 화면은 없다. 주문 제출이 곧 주문 완료이다.

### 4.6 동작과 상태

| 상황 | 동작 |
| --- | --- |
| 초기 진입 | 메뉴 목록을 불러와 카드를 그린다. 장바구니는 비어 있다. |
| 메뉴 로딩 실패 | 메뉴 영역에 오류 안내를 보여 준다. |
| 메뉴가 없음 | 메뉴 영역에 빈 상태 안내를 보여 준다. |
| 품절/판매 불가 | 해당 카드의 담기 버튼을 비활성화하고, 담을 수 없음을 표시한다. |
| 주문 성공 | 장바구니를 비우고 완료 안내를 보여 준다. |
| 페이지 새로고침 | 장바구니는 유지하지 않아도 된다 (메모리만 사용). |

### 4.7 포함하지 않는 것
- 로그인, 회원가입, 결제
- 장바구니 수량 직접 수정, 개별 삭제 UI (1차 범위 밖. 필요하면 이후 추가)
- 메뉴 검색, 카테고리 필터
- 주문 내역 조회 (고객용)

## 5. 프런트엔드 UI — 관리자 화면

### 5.1 화면 목적
관리자가 주문 요약, 메뉴 재고, 개별 주문 상태를 한 화면에서 보고 처리하는 화면이다. 인증 없이 헤더의 `관리자`로 진입한다.

### 5.2 레이아웃
화면은 위에서 아래로 다음 네 영역으로 구성한다.

1. **헤더**: 앱 이름과 화면 전환 내비게이션
2. **관리자 대시보드**: 주문 건수 요약
3. **재고 현황**: 메뉴별 재고 카드
4. **주문 현황**: 주문 목록과 상태 변경

헤더는 화면 상단에 고정한다. 대시보드·재고·주문 현황은 한 페이지에서 세로로 스크롤한다.

### 5.3 헤더
- 왼쪽: 앱 이름 `COZY`
- 오른쪽: `주문하기`, `관리자` 두 개의 내비게이션
- 현재 화면이 관리자일 때 `관리자`를 활성 상태로 표시한다 (테두리 등)
- `주문하기` 또는 앱 이름을 누르면 주문하기 화면으로 이동한다
- `관리자`를 눌러도 관리자 화면에 머문다

### 5.4 관리자 대시보드
제목은 `관리자 대시보드`이다. 한 줄에 다음 네 숫자를 보여 준다.

| 항목 | 의미 |
| --- | --- |
| 총 주문 | 화면에 집계하는 주문의 전체 건수 |
| 주문 접수 | 상태가 `주문 접수`인 건수 |
| 제조 중 | 상태가 `제조 중`인 건수 |
| 제조 완료 | 상태가 `제조 완료`인 건수 |

- `총 주문` = `주문 접수` + `제조 중` + `제조 완료`
- 숫자는 주문 현황과 항상 같게 맞춘다. 주문 상태가 바뀌면 대시보드도 바로 갱신한다.
- 1차 범위에서는 날짜 필터 없이 저장된 주문 전체를 집계한다.

### 5.5 재고 현황
제목은 `재고 현황`이다. 메뉴마다 카드를 가로로 나열하고, 화면이 좁으면 다음 줄로 넘어간다.

각 카드는 다음을 포함한다.

| 요소 | 설명 |
| --- | --- |
| 메뉴명 | 예: 아메리카노 (ICE), 아메리카노 (HOT), 카페라떼 |
| 재고 수량 | `{n}개` 형식 |
| − / + | 재고를 1씩 줄이거나 늘린다 |

- `−`를 누르면 재고가 1 줄어든다. 0 미만으로는 내려가지 않으며, 0일 때 `−`는 비활성화한다.
- `+`를 누르면 재고가 1 늘어난다.
- 재고 변경에 실패하면 수량을 되돌리고 오류 안내를 보여 준다.
- 재고가 0인 메뉴는 주문하기 화면에서 품절(담기 불가)로 취급한다. (4.6 참고)
- 손님이 주문에 성공하면 해당 메뉴 재고는 주문 수량만큼 줄어든다. 재고가 부족한 메뉴가 있으면 그 주문은 실패한다.
- 이 화면에서 메뉴 추가·삭제·이름·가격·옵션 편집은 하지 않는다.

### 5.6 주문 현황
제목은 `주문 현황`이다. 주문을 최신순으로 한 줄씩 보여 준다.

#### 주문 한 줄
- 왼쪽: 주문 시각 (`M월 D일 HH:MM`), 메뉴별 `{메뉴명} x {수량}` (옵션이 있으면 주문하기 장바구니와 같은 형식으로 표시), 주문 총 금액
- 오른쪽: 현재 상태를 나타내는 버튼

주문이 없으면 빈 상태 안내를 보여 준다. 목록을 불러오지 못하면 오류 안내를 보여 준다.

#### 주문 상태
새 주문은 `주문 접수`로 시작한다. 버튼을 누르면 다음 상태로 한 단계만 진행한다.

| 현재 상태 | 버튼 라벨 | 누르면 |
| --- | --- | --- |
| 주문 접수 | 주문 접수 | `제조 중`으로 변경 |
| 제조 중 | 제조 중 | `제조 완료`로 변경 |
| 제조 완료 | 제조 완료 | 더 이상 바꾸지 않는다. 버튼은 비활성화한다 |

- 상태를 되돌리거나 주문을 취소·삭제하는 UI는 없다.
- 변경에 실패하면 이전 상태를 유지하고 오류 안내를 보여 준다.

### 5.7 동작과 상태

| 상황 | 동작 |
| --- | --- |
| 초기 진입 | 대시보드, 재고, 주문 목록을 불러와 그린다. |
| 데이터 로딩 실패 | 해당 영역에 오류 안내를 보여 준다. |
| 재고 변경 성공 | 해당 카드 수량을 갱신한다. |
| 주문 상태 변경 성공 | 해당 줄의 버튼과 대시보드 숫자를 갱신한다. |
| 다른 화면에서 주문이 생긴 뒤 복귀 | 관리자 화면에 다시 들어오면 최신 데이터를 불러온다. |

### 5.8 포함하지 않는 것
- 관리자 로그인·권한
- 날짜·상태 필터, 검색
- 주문 취소·되돌리기
- 메뉴 CRUD (추가·삭제·가격·옵션 편집)
- 실시간 푸시(웹소켓). 화면을 열거나 조작할 때 조회·갱신하면 된다.

## 6. 백엔드

프런트엔드와 분리해 Node.js Express + PostgreSQL로 개발한다. 인증·결제는 없다. API 기본 경로 예: `http://localhost:3000/api`

### 6.1 데이터 모델

세 개의 테이블을 사용한다.

#### Menus
커피 메뉴 기본 정보와 재고.

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | SERIAL PK | 메뉴 ID |
| name | TEXT NOT NULL | 커피 이름. 예: 아메리카노(ICE) |
| description | TEXT | 짧은 설명 |
| price | INTEGER NOT NULL | 기본 가격(원) |
| image_url | TEXT | 이미지 경로 또는 URL |
| stock | INTEGER NOT NULL DEFAULT 0 | 재고 수량. 0 미만 불가 |

- 주문하기 화면: 이름, 설명, 가격, 이미지, (품절 판단용) 재고
- 관리자 재고 현황: 이름과 재고 수량

#### Options
메뉴에 붙는 선택 옵션.

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | SERIAL PK | 옵션 ID |
| name | TEXT NOT NULL | 옵션 이름. 예: 샷 추가 |
| price | INTEGER NOT NULL | 옵션 추가 금액(원). 0 가능 |
| menu_id | INTEGER NOT NULL FK → menus.id | 연결할 메뉴 |

한 메뉴에 옵션이 여러 개일 수 있다. 옵션이 없는 메뉴도 있다.

#### Orders
접수된 주문.

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | SERIAL PK | 주문 ID |
| ordered_at | TIMESTAMPTZ NOT NULL | 주문 일시. 저장 시각 |
| items | JSONB NOT NULL | 주문 내용. 아래 구조 |
| total_amount | INTEGER NOT NULL | 주문 총 금액(원) |
| status | TEXT NOT NULL | `received` / `preparing` / `done` |

`status` 화면 표시:

| 값 | 화면 |
| --- | --- |
| received | 주문 접수 |
| preparing | 제조 중 |
| done | 완료 |

`items` 한 줄 예시:

```json
{
  "menuId": 1,
  "name": "아메리카노(ICE)",
  "quantity": 1,
  "options": [{ "id": 1, "name": "샷 추가", "price": 500 }],
  "amount": 4500
}
```

- `amount` = (메뉴 기본 가격 + 선택한 옵션 가격 합) × 수량
- `total_amount` = 모든 줄 `amount`의 합
- 주문 시점의 이름·가격을 그대로 저장한다. 이후 메뉴가 바뀌어도 주문 내용은 유지한다.

신규 주문의 `status`는 항상 `received`(주문 접수)이다.

### 6.2 스키마를 쓰는 사용자 흐름

#### a. 메뉴 조회
주문하기 화면(또는 앱 진입)에서 Menus를 읽어 브라우저에 보여 준다. 각 메뉴의 Options도 함께 내려 체크박스를 그린다. 재고 수량(`stock`)은 관리자 화면 재고 현황에 숫자로 보여 준다. 주문하기 화면에서는 재고가 0이면 품절로만 쓰고, 개수는 강조하지 않는다.

#### b. 장바구니
사용자가 커피와 옵션을 골라 담는다. 장바구니는 브라우저 메모리에만 둔다. 서버에 저장하지 않는다.

#### c. 주문 저장
장바구니에서 주문하기를 누르면 주문 정보를 Orders에 저장한다. 주문일시(`ordered_at`)와 주문내용(`items`: 메뉴, 수량, 옵션, 금액)과 총 금액을 담는다. 같은 요청에서 주문한 메뉴 수량만큼 Menus.stock을 줄인다. 재고가 부족하면 주문을 만들지 않고 실패한다.

#### d. 관리자 주문 현황
Orders를 최신순으로 주문 현황에 보여 준다. 기본 상태는 주문 접수이다. 상태 버튼을 누르면 한 단계만 진행한다.

- 주문 접수(`received`) 클릭 → 제조 중(`preparing`)
- 제조 중(`preparing`) 클릭 → 완료(`done`)
- 완료는 더 이상 바꾸지 않는다

관리자 대시보드 숫자는 Orders의 상태별 건수이다.

### 6.3 API 설계

공통: JSON, 실패 시 `{ "error": "메시지" }`

#### 커피 메뉴 목록
주문하기를 눌러 화면을 열 때 데이터베이스에서 커피 메뉴를 불러온다.

`GET /api/menus`

응답 예:

```json
[
  {
    "id": 1,
    "name": "아메리카노(ICE)",
    "description": "얼음과 함께 시원하게 즐기는 아메리카노",
    "price": 4000,
    "imageUrl": "/menus/americano-ice.png",
    "stock": 10,
    "options": [
      { "id": 1, "name": "샷 추가", "price": 500 },
      { "id": 2, "name": "시럽 추가", "price": 0 }
    ]
  }
]
```

관리자 재고 현황도 이 목록의 `stock`을 사용한다.

#### 주문 생성
사용자가 커피를 고른 뒤 주문하기를 누르면 주문 정보를 저장하고, 그에 맞춰 메뉴 재고를 수정한다.

`POST /api/orders`

요청 예:

```json
{
  "items": [
    {
      "menuId": 1,
      "quantity": 1,
      "optionIds": [1]
    },
    {
      "menuId": 2,
      "quantity": 2,
      "optionIds": []
    }
  ]
}
```

서버 처리:

1. 메뉴·옵션이 존재하는지 확인한다. 옵션은 해당 메뉴에 속해야 한다.
2. 메뉴별로 수량을 합쳐 재고와 비교한다. 부족하면 400으로 거절하고 DB는 바꾸지 않는다.
3. 줄 금액과 총 금액을 서버에서 계산한다. 클라이언트가 보낸 금액은 쓰지 않는다.
4. Orders에 `ordered_at`(현재 시각), `items`, `total_amount`, `status: "received"`를 저장한다.
5. 주문 수량만큼 Menus.stock을 줄인다.
6. 생성한 주문을 201로 반환한다.

#### 주문 단건 조회
주문 ID를 전달하면 해당 주문 정보를 보여 준다.

`GET /api/orders/:id`

없으면 404.

응답 예:

```json
{
  "id": 12,
  "orderedAt": "2026-07-31T13:00:00.000Z",
  "status": "received",
  "totalAmount": 12500,
  "items": [
    {
      "menuId": 1,
      "name": "아메리카노(ICE)",
      "quantity": 1,
      "options": [{ "id": 1, "name": "샷 추가", "price": 500 }],
      "amount": 4500
    }
  ]
}
```

#### 주문 목록 조회
관리자 주문 현황용. 최신순.

`GET /api/orders`

#### 주문 상태 변경
주문 접수 → 제조 중 → 완료. 한 단계만 진행한다. 완료이거나 잘못된 전이면 400.

`PATCH /api/orders/:id/status`

요청 예: `{ "status": "preparing" }`

#### 재고 수정
관리자 재고 현황의 + / −. 수량은 0 미만이 될 수 없다.

`PATCH /api/menus/:id/stock`

요청 예: `{ "stock": 11 }` 또는 `{ "delta": 1 }`

둘 중 하나를 지원하면 된다. 없는 메뉴는 404.

### 6.4 포함하지 않는 것
- 로그인, 권한, 결제
- 장바구니 서버 저장
- 메뉴·옵션 추가/삭제 API (초기 데이터는 DB 시드로 넣는다)
- 주문 취소·상태 되돌리기

