# COZY UI

커피 주문 앱 프런트엔드. React + JavaScript + Vite.

## 실행

Node.js는 `D:\Documents\nodejs\` 를 사용한다.

```bash
cd ui
npm install
npm run dev
```

브라우저에서 http://localhost:5173 을 연다.

```bash
npm run build    # 프로덕션 빌드
npm run preview  # 빌드 결과 미리보기
```
